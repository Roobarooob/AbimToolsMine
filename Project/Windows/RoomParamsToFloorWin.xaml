<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AbimToolsMine.RoomParamsToFloorWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Передача параметров из помещений в элементы отделки"
        Width="680" Height="620" MinWidth="560" MinHeight="480"
        ResizeMode="CanResizeWithGrip" WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterOwner" Background="#F5F6F8"
        FontFamily="Segoe UI" FontSize="12" Topmost="True">

    <Window.Resources>
        <SolidColorBrush x:Key="TextBrush" Color="#2C3E50"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#66727E"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#D8DDE3"/>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#27AE60"/>

        <Style x:Key="CardGroupBoxStyle" TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="White" BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1" CornerRadius="5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Padding="12,8" Background="#F8F9FA"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                                        CornerRadius="5,5,0,0">
                                    <ContentPresenter ContentSource="Header"/>
                                </Border>
                                <ContentPresenter Grid.Row="1" Margin="12,10" TextElement.FontWeight="Normal"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ButtonBaseStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="92"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="#C8CED6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#EEF3F7"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#9EABB7"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.72"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="PrimaryBorder" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PrimaryBorder" Property="Opacity" Value="0.86"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="PrimaryBorder" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GridTextStyle" TargetType="TextBlock">
            <Setter Property="Padding" Value="7,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style x:Key="GridEditStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#3498DB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="8,1,0,9"
                   Text="Соответствие параметров: Помещение → Элемент отделки"
                   FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>

        <DataGrid Grid.Row="1" x:Name="DgMappings" Margin="0,0,0,9"
                  AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True"
                  CanUserReorderColumns="False" CanUserSortColumns="False"
                  HeadersVisibility="Column" GridLinesVisibility="None" RowHeaderWidth="0"
                  RowHeight="25" ColumnHeaderHeight="30"
                  BorderThickness="1" BorderBrush="{StaticResource BorderBrush}"
                  Background="White" RowBackground="White" AlternatingRowBackground="#F8F9FA">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F8F9FA"/>
                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Параметр помещения (источник)"
                                    Binding="{Binding SourceParam, UpdateSourceTrigger=PropertyChanged}"
                                    ElementStyle="{StaticResource GridTextStyle}"
                                    EditingElementStyle="{StaticResource GridEditStyle}" Width="*"/>
                <DataGridTextColumn Header="Параметр элемента отделки (цель)"
                                    Binding="{Binding TargetParam, UpdateSourceTrigger=PropertyChanged}"
                                    ElementStyle="{StaticResource GridTextStyle}"
                                    EditingElementStyle="{StaticResource GridEditStyle}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="2" Header="Категории элементов отделки"
                  Style="{StaticResource CardGroupBoxStyle}" Margin="0,0,0,9">
            <StackPanel Orientation="Horizontal">
                <CheckBox x:Name="CbFloors" Content="Полы" IsChecked="True"
                          VerticalAlignment="Center" Margin="0,0,24,0"/>
                <CheckBox x:Name="CbCeilings" Content="Потолки" IsChecked="True"
                          VerticalAlignment="Center" Margin="0,0,24,0"/>
                <CheckBox x:Name="CbWalls" Content="Стены" IsChecked="True"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Помещения"
                  Style="{StaticResource CardGroupBoxStyle}" Margin="0,0,0,9">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <RadioButton x:Name="RbAllRooms" Content="Все помещения" IsChecked="True"
                                 VerticalAlignment="Center" Checked="OnRoomModeChanged"/>
                    <RadioButton x:Name="RbSelectedRooms" Content="Выбранные помещения"
                                 Margin="20,0,0,0" VerticalAlignment="Center"
                                 Checked="OnRoomModeChanged"/>
                </StackPanel>
                <StackPanel x:Name="PanelRoomPick" Grid.Row="1" Orientation="Horizontal"
                            Margin="0,9,0,0" Visibility="Collapsed">
                    <Button x:Name="BtnPickRooms" Content="Выбрать на виде..."
                            Margin="0,0,12,0" Style="{StaticResource ButtonBaseStyle}"
                            Click="OnPickRoomsClick"/>
                    <TextBlock x:Name="TbRoomCount" VerticalAlignment="Center"
                               FontStyle="Italic" Foreground="{StaticResource MutedTextBrush}"
                               Text="Помещения не выбраны"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <Grid Grid.Row="4" Margin="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TbStatus" Grid.Column="0" Margin="4,0,12,0"
                       Foreground="#C0392B" FontStyle="Italic" FontSize="11"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Запустить"
                        Style="{StaticResource PrimaryButtonStyle}" Click="OnRunClick"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
