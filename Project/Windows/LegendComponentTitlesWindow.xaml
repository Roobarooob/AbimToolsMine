<Window x:Class="AbimToolsMine.LegendComponentTitlesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заголовки в легендах"
        Height="270"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Параметр типоразмера" Margin="0,0,0,6"/>
        <TextBox x:Name="ParameterNameBox"
                 Grid.Row="1"
                 Height="26"
                 VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="2" Text="Отступ, мм" Margin="0,10,0,6"/>
        <TextBox x:Name="OffsetBox"
                 Grid.Row="3"
                 Height="26"
                 Text="200"
                 VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="4" Text="Тип текстовой метки" Margin="0,10,0,6"/>
        <ComboBox x:Name="TextNoteTypeBox"
                  Grid.Row="5"
                  Height="26"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="ОК" Width="90" Margin="0,0,8,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Отмена" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
